const Section = ({ children }) => {
  return (
    <section className='mx-4 my-4 max-w-7xl md:mx-auto rounded-md overflow-hidden'>
      {children}
    </section>
  );
};

export default Section;
