const SectionTitle = ({ text }) => {
  return (
    <h2 className='uppercase tracking-wide font-mulish text-lg text-zinc-500 font-extrabold'>
      {text}
    </h2>
  );
};

export default SectionTitle;
